language: node_js
cache: npm
install:
  - npm ci
  - npx cypress verify
  - npm install -D wait-on
script:
  - npm run start & wait-on http://localhost:8080 &
  - npm run test
  - kill $(jobs -p) || true

